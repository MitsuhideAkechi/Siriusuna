[字符串创建方式](https://www.doubao.com/chat/collection/20749431053745666?type=Thread)

在 Ruby 中，**`?` 作为前缀**时，用于构造**单个字符的字符串字面量**。
当你输入 `?.` 时：
- `?` 是 “单字符字符串” 的前缀标记，
- 后面的 `.` 是要表示的字符本身。
因此，`?.` 会被解析为**包含字符 `.` 的单字符字符串**，即 `"."`。

# Methods
![](IMG-20251210185505257.png)
![](IMG-20251210185505287.png)
